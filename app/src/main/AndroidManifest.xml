<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.material.rally">

  <application
      android:name=".ui.RallyApp"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Rally.DayNight"
      tools:ignore="GoogleAppIndexingWarning">

    <activity android:name=".ui.MainActivity"
        android:theme="@style/Rally.DayNight.Main">
    </activity>
    <activity
        android:name=".ui.detail.DetailActivity"
        android:theme="@style/Rally.DayNight.Detail">
    </activity>
    <activity android:name=".ui.login.LoginActivity"
        android:theme="@style/Rally.DayNight.Login">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>